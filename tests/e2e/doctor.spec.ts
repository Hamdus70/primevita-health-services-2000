import { test, expect } from '@playwright/test';

test.describe('Doctor User Flow', () => {
  test('login success', async ({ page }) => {
    expect(true).toBeTruthy();
  });

  test('write SOAP note for patient', async ({ page }) => {
    expect(true).toBeTruthy();
  });

  test('prescribe medication', async ({ page }) => {
    expect(true).toBeTruthy();
  });

  test('review AI insights on patient data', async ({ page }) => {
    expect(true).toBeTruthy();
  });
});
